<script setup lang="ts">
import { computed, ref } from "vue"; import { useRoute } from "vue-router";
const route = useRoute(); const enabled = ref(false); const query = ref(""); const page = computed(() => String(route.path.split("/").pop()));
const titles: Record<string,string> = { automation: "自动化规则", executions: "执行记录与 Trace", events: "事件与失败队列", sync: "导入与同步", plugins: "插件与连接器" };
</script>
<template><main class="p-4 md:p-6"><div class="flex justify-between items-start mb-6"><div><h1 class="text-2xl font-semibold">{{ titles[page] || '集成与自动化' }}</h1><p class="mt-1 text-[var(--el-text-color-secondary)]">外部连接、规则和任务均受权限与审计策略约束。</p></div><el-button>刷新</el-button></div><el-card shadow="never" class="mb-5"><div class="grid grid-cols-1 md:grid-cols-4 gap-4"><div v-for="item in ['已启用连接器','待处理事件','失败任务','最近执行']" :key="item"><div class="text-sm text-[var(--el-text-color-secondary)]">{{ item }}</div><div class="text-2xl font-semibold mt-2">—</div></div></div></el-card><el-card shadow="never"><template #header><div class="flex justify-between items-center"><span>{{ titles[page] }}</span><div class="flex gap-2"><el-input v-model="query" placeholder="搜索名称或 ID" clearable class="w-56"/><el-switch v-model="enabled" active-text="仅启用"/></div></div></template><el-empty description="集成后端目录接口尚未接入；此处保留规则构建器、执行详情、失败队列和插件权限布局"/><el-alert title="不会在前端伪造外部连接器状态或执行结果。" type="info" show-icon :closable="false"/></el-card></main></template>
