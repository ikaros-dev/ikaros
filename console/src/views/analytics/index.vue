<script setup lang="ts">
import { computed, ref } from "vue"; import { useRoute } from "vue-router";
const route = useRoute(); const range = ref("7d"); const tab = computed(() => ["analytics", "overview"].includes(String(route.path.split("/").pop())) ? "overview" : String(route.path.split("/").pop()));
const labels: Record<string,string> = { overview: "个人概览", content: "内容分析", storage: "存储分析", planning: "效率分析", system: "系统历史", metrics: "指标目录", reports: "报表与重建" };
</script>
<template><main class="p-4 md:p-6"><div class="flex justify-between items-start mb-6"><div><h1 class="text-2xl font-semibold">{{ labels[tab] || '数据分析' }}</h1><p class="mt-1 text-[var(--el-text-color-secondary)]">按授权范围查看聚合指标；不会展示私密内容或未经后端确认的推断数据。</p></div><el-select v-model="range" class="w-32"><el-option label="近 7 天" value="7d"/><el-option label="近 30 天" value="30d"/><el-option label="近 90 天" value="90d"/></el-select></div><div class="grid grid-cols-1 md:grid-cols-4 gap-4 mb-5"><el-card v-for="label in ['对象数量','活跃用户','存储用量','任务完成率']" :key="label" shadow="never"><div class="text-sm text-[var(--el-text-color-secondary)]">{{ label }}</div><div class="text-2xl font-semibold mt-2">—</div><div class="text-xs mt-2 text-[var(--el-text-color-secondary)]">等待分析接口提供聚合数据</div></el-card></div><el-card shadow="never"><el-empty description="分析数据接口尚未接入；页面保留指标目录、图表和报表布局，避免生成虚假数据"/><el-alert title="当前后端未提供 analytics/metrics/reports 查询接口。" type="info" show-icon :closable="false"/></el-card></main></template>
