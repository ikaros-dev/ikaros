package run.ikaros.finance; import java.math.BigDecimal; import java.time.Instant; import java.util.UUID; import org.springframework.data.annotation.Id; import org.springframework.data.annotation.Version; import org.springframework.data.relational.core.mapping.Table; @Table("finance_exchange_rate") public record ExchangeRateEntity(@Id UUID id,UUID ledgerId,UUID ownerId,String fromCurrency,String toCurrency,BigDecimal rate,Instant effectiveAt,String provider,ExchangeRateSource source,Instant createdAt,@Version Long version) {}
