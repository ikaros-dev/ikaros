package run.ikaros.finance; import java.math.BigDecimal; import java.time.Instant; import java.util.UUID; import org.springframework.data.annotation.Id; import org.springframework.data.annotation.Version; import org.springframework.data.relational.core.mapping.Table; @Table("finance_transaction") public record FinanceTransactionEntity(@Id UUID id,UUID ledgerId,UUID ownerId,TransactionType type,UUID accountId,UUID targetAccountId,BigDecimal amount,String currency,UUID categoryId,String payee,String note,Instant occurredAt,TransactionStatus status,String source,Instant createdAt,@Version Long version) {}
