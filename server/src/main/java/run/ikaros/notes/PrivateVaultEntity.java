package run.ikaros.notes; import java.time.Instant; import java.util.UUID; import org.springframework.data.annotation.Id; import org.springframework.data.annotation.Version; import org.springframework.data.relational.core.mapping.Table; @Table("private_vault") public record PrivateVaultEntity(@Id UUID id,UUID ownerId,String name,String securityProfile,String cryptoContext,String encryptedRecoveryKey,VaultStatus status,Instant createdAt,Instant updatedAt,@Version Long version) {}
