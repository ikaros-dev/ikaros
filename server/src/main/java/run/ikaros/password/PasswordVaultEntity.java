package run.ikaros.password; import java.time.Instant; import java.util.UUID; import org.springframework.data.annotation.Id; import org.springframework.data.annotation.Version; import org.springframework.data.relational.core.mapping.Table; @Table("password_vault") public record PasswordVaultEntity(@Id UUID id,UUID ownerId,String name,String securityProfile,String cryptoContext,String encryptedRecoveryKey,PasswordVaultStatus status,Instant createdAt,Instant updatedAt,@Version Long version) {}
