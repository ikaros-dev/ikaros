package run.ikaros.reading; import java.time.Instant; import java.util.UUID; import org.springframework.data.annotation.Id; import org.springframework.data.annotation.Version; import org.springframework.data.relational.core.mapping.Table; import org.springframework.data.relational.core.mapping.Column;
@Table("reading_session") public record ReadingSessionEntity(@Id UUID id,@Column("owner_id") UUID ownerId,@Column("work_id") UUID workId,@Column("edition_id") UUID editionId,@Column("started_chapter_id") UUID startedChapterId,@Column("current_chapter_id") UUID currentChapterId,@Column("started_locator_kind") String startedLocatorKind,@Column("started_locator_value") String startedLocatorValue,@Column("current_locator_kind") String currentLocatorKind,@Column("current_locator_value") String currentLocatorValue,Instant startedAt,@Column("last_active_at") Instant lastActiveAt,@Column("ended_at") Instant endedAt,boolean offline,@Version Long version) {}
