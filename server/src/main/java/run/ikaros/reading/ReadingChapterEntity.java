package run.ikaros.reading;
import java.time.Instant; import java.util.UUID; import org.springframework.data.annotation.Id; import org.springframework.data.annotation.Version; import org.springframework.data.relational.core.mapping.Table; import org.springframework.data.relational.core.mapping.Column;
@Table("reading_chapter") public record ReadingChapterEntity(@Id UUID id,@Column("owner_id") UUID ownerId,@Column("work_id") UUID workId,@Column("edition_id") UUID editionId,@Column("volume_id") UUID volumeId,@Column("resource_id") UUID resourceId,@Column("structured_number") String structuredNumber,@Column("display_label") String displayLabel,String title,@Column("sort_order") int sortOrder,@Column("content_kind") String contentKind,@Column("page_count") Integer pageCount,@Column("text_length") Integer textLength,String availability,String lifecycle,@Version Long version) {}
