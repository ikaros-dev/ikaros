package run.ikaros.planning; import java.time.Instant; import java.util.UUID; import org.springframework.data.annotation.Id; import org.springframework.data.annotation.Version; import org.springframework.data.relational.core.mapping.Column; import org.springframework.data.relational.core.mapping.Table;
@Table("planning_comment") public record PlanningCommentEntity(@Id UUID id,@Column("author_id") UUID authorId,@Column("target_type") PlanningCommentTargetType targetType,@Column("target_id") UUID targetId,String content,@Column("created_at") Instant createdAt,@Column("updated_at") Instant updatedAt,@Column("deleted_at") Instant deletedAt,@Version Long version) {}
