package run.ikaros.password; import java.time.Instant; import java.util.UUID; import org.springframework.data.annotation.Id; import org.springframework.data.annotation.Version; import org.springframework.data.relational.core.mapping.Table; @Table("password_vault_item") public record PasswordVaultItemEntity(@Id UUID id,UUID vaultId,UUID ownerId,String itemType,String encryptedName,String encryptedPayload,String encryptedCustomFields,boolean favorite,long revision,boolean tombstone,Instant createdAt,Instant updatedAt,@Version Long version) {}
