package run.ikaros.music;
import java.time.Instant; import java.util.UUID; import org.springframework.data.annotation.Id; import org.springframework.data.annotation.Version; import org.springframework.data.relational.core.mapping.Column; import org.springframework.data.relational.core.mapping.Table;
@Table("music_metadata_candidate") public record MusicMetadataCandidateEntity(@Id UUID id,@Column("owner_id") UUID ownerId,@Column("track_id") UUID trackId,@Column("attachment_id") UUID attachmentId,String title,String artist,String album,@Column("album_artist") String albumArtist,String isrc,String genre,@Column("track_number") Integer trackNumber,@Column("disc_number") Integer discNumber,@Column("release_year") String releaseYear,String source,@Column("created_at") Instant createdAt,@Column("updated_at") Instant updatedAt,@Version Long version) {}
