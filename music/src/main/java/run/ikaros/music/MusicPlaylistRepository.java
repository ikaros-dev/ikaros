package run.ikaros.music; import java.util.UUID; import org.springframework.data.repository.reactive.ReactiveCrudRepository; import reactor.core.publisher.Flux; public interface MusicPlaylistRepository extends ReactiveCrudRepository<MusicPlaylistEntity,UUID>{Flux<MusicPlaylistEntity> findAllByOwnerIdOrderByUpdatedAtDesc(UUID ownerId);}
