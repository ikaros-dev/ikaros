package run.ikaros.photo; import java.time.Instant; import java.util.UUID; import org.springframework.data.annotation.Id; import org.springframework.data.annotation.Version; import org.springframework.data.relational.core.mapping.Table; import org.springframework.data.relational.core.mapping.Column;
@Table("photo") public record PhotoEntity(@Id UUID id,@Column("owner_id") UUID ownerId,@Column("resource_id") UUID resourceId,@Column("capture_time") Instant captureTime,@Column("capture_time_local") String captureTimeLocal,@Column("time_zone") String timeZone,Integer width,Integer height,Integer orientation,@Column("camera_make") String cameraMake,@Column("camera_model") String cameraModel,@Column("lens_model") String lensModel,String latitude,String longitude,@Version Long version) {}
