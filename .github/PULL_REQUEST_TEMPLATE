<!--
    ⚠️ Please make sure to read this template first, pull requests that don't accord with this template
    maybe closed without notice.
    Texts surrounded by `<` and `>` are meant to be replaced by you, e.g. <framework name>, <issue number>.
    Put an `x` in the `[ ]` to mark the item as CHECKED. `[x]`
-->

<!-- ==== 🐛 Remove this line WHEN AND ONLY WHEN you're fixing a bug, follow the checklist 👇 ====
### Fix <bug description or the bug issue number or bug issue link>
- [ ] Add a unit test to verify that the fix works.
- [ ] Explain briefly why the bug exists and how to fix it.
     ==== 🐛 Remove this line WHEN AND ONLY WHEN you're fixing a bug, follow the checklist 👆 ==== -->

<!-- ==== 📈 Remove this line WHEN AND ONLY WHEN you're improving the performance, follow the checklist 👇 ====
### Improve the performance of <class or module or ...>
- [ ] Add a benchmark for the improvement, refer to [the existing ones](https://github.com/ikaros-dev/ikaros/tree/develop/src/test/java/cn/liguohao/ikaros/datacarrier/LinkedArrayBenchmark.java)
- [ ] The benchmark result.
```text
<Paste the benchmark results here>
```
- [ ] Links/URLs to the theory proof or discussion articles/blogs. <links/URLs here>
     ==== 📈 Remove this line WHEN AND ONLY WHEN you're improving the performance, follow the checklist 👆 ==== -->

<!-- ==== 🆕 Remove this line WHEN AND ONLY WHEN you're adding a new feature, follow the checklist 👇 ====
### <Feature description>
- [ ] If this is non-trivial feature, paste the links/URLs to the design doc.
- [ ] Update the documentation to include this new feature.
- [ ] Tests(including UT, IT, E2E) are added to verify the new feature.
- [ ] If it's UI related, attach the screenshots below.
     ==== 🆕 Remove this line WHEN AND ONLY WHEN you're adding a new feature, follow the checklist 👆 ==== -->
- [ ] Update the [`CHANGES` log](https://github.com/ikaros-dev/ikaros/blob/develop/CHANGELOG.MD).
- [ ] If this pull request closes/resolves/fixes an existing issue, replace the issue number. Closes #<issue number>.

<!-- Required for Console or Console-contract changes. Use N/A only when this PR does not touch Console. -->
### Console Golden Path
- Golden Path 关联：<GP01-GP09 或 N/A>
- 旅程覆盖：<入口、加载/空状态、成功、失败、无权限、后台进度、恢复/重试、最终业务结果中的适用段落>
- 新的用户概念：<无，或说明新增概念>
- 成功验证：<真实用户结果和证据>
- 失败验证：<真实失败状态和恢复/重试证据>
- 无权限验证：<直接 canonical URL 与导航收敛证据，或 N/A>
- 后台进度验证：<Activity 观察证据，或 N/A>
- 内部实现词汇：<是否仍暴露；若是说明原因和 Advanced 边界>
