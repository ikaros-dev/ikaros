package run.ikaros.document; import java.time.Instant; import java.util.UUID; import org.springframework.data.annotation.Id; import org.springframework.data.annotation.Version; import org.springframework.data.relational.core.mapping.Table; import org.springframework.data.relational.core.mapping.Column;
@Table("document_working_copy") public record DocumentWorkingCopyEntity(@Id UUID id,@Column("document_id") UUID documentId,@Column("owner_id") UUID ownerId,String content,@Column("content_schema_version") String contentSchemaVersion,@Column("base_revision_id") UUID baseRevisionId,Instant updatedAt,@Version Long version) {}
