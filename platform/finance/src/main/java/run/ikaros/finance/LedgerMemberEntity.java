package run.ikaros.finance; import java.time.Instant; import java.util.UUID; import org.springframework.data.annotation.Id; import org.springframework.data.annotation.Version; import org.springframework.data.relational.core.mapping.Table; @Table("finance_ledger_member") public record LedgerMemberEntity(@Id UUID id,UUID ledgerId,UUID principalId,UUID addedBy,LedgerMemberRole role,Instant createdAt,@Version Long version) {}
