package run.ikaros.finance; import java.math.BigDecimal; import java.time.Instant; import java.time.YearMonth; import java.util.UUID; import org.springframework.data.annotation.Id; import org.springframework.data.annotation.Version; import org.springframework.data.relational.core.mapping.Table; @Table("finance_budget") public record BudgetEntity(@Id UUID id,UUID ledgerId,UUID ownerId,UUID categoryId,YearMonth month,BigDecimal amount,Instant createdAt,@Version Long version) {}
