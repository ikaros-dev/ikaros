package run.ikaros.music; import java.util.UUID; import org.springframework.data.repository.reactive.ReactiveCrudRepository; import reactor.core.publisher.Flux; public interface MusicPlaylistEntryRepository extends ReactiveCrudRepository<MusicPlaylistEntryEntity,UUID>{Flux<MusicPlaylistEntryEntity> findAllByPlaylistIdOrderByPositionAsc(UUID playlistId);}
