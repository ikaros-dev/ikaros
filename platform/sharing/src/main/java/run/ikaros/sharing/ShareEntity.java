package run.ikaros.sharing; import java.time.Instant; import java.util.UUID; import org.springframework.data.annotation.Id; import org.springframework.data.annotation.Version; import org.springframework.data.relational.core.mapping.Table; import org.springframework.data.relational.core.mapping.Column;
@Table("share_grant") public record ShareEntity(@Id UUID id,@Column("issuer_id") UUID issuerId,@Column("target_type") String targetType,@Column("target_id") UUID targetId,@Column("grantee_type") ShareGranteeType granteeType,@Column("grantee_id") UUID granteeId,String capabilities,@Column("token_digest") String tokenDigest,@Column("expires_at") Instant expiresAt,ShareStatus status,@Column("created_at") Instant createdAt,@Column("updated_at") Instant updatedAt,@Version Long version) {}
