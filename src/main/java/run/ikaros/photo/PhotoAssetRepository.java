package run.ikaros.photo; import java.util.UUID; import org.springframework.data.repository.reactive.ReactiveCrudRepository; import reactor.core.publisher.Flux; import reactor.core.publisher.Mono; public interface PhotoAssetRepository extends ReactiveCrudRepository<PhotoAssetEntity,UUID>{Flux<PhotoAssetEntity> findAllByPhotoId(UUID photoId);Mono<PhotoAssetEntity> findByPhotoIdAndRole(UUID photoId,PhotoAssetRole role);}
