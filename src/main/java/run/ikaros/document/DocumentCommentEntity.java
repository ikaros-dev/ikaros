package run.ikaros.document; import java.time.Instant; import java.util.UUID; import org.springframework.data.annotation.Id; import org.springframework.data.annotation.Version; import org.springframework.data.relational.core.mapping.Table; import org.springframework.data.relational.core.mapping.Column;
@Table("document_comment") public record DocumentCommentEntity(@Id UUID id,@Column("document_id") UUID documentId,@Column("author_id") UUID authorId,@Column("parent_comment_id") UUID parentCommentId,@Column("anchor_revision_id") UUID anchorRevisionId,@Column("anchor") String anchor,String body,DocumentCommentStatus status,Instant createdAt,Instant updatedAt,@Version Long version) {}
