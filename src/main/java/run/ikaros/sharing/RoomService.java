package run.ikaros.sharing; import java.util.UUID; import reactor.core.publisher.Flux; import reactor.core.publisher.Mono; public interface RoomService{Mono<RoomView> create(UUID actor,CreateRoomRequest request);Flux<RoomView> list(UUID actor);Mono<RoomView> lock(UUID actor,UUID roomId);Mono<RoomView> unlock(UUID actor,UUID roomId);Mono<RoomView> end(UUID actor,UUID roomId);Mono<RoomMembershipView> join(UUID actor,UUID roomId);Mono<RoomMembershipView> leave(UUID actor,UUID roomId);Flux<RoomMembershipView> members(UUID actor,UUID roomId);Mono<RoomMembershipView> changeRole(UUID actor,UUID roomId,UUID principalId,RoomRole role);Mono<RoomMembershipView> transferOwnership(UUID actor,UUID roomId,UUID principalId);Mono<RoomMembershipView> remove(UUID actor,UUID roomId,UUID principalId);}
