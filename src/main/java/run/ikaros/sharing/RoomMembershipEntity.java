package run.ikaros.sharing; import java.time.Instant; import java.util.UUID; import org.springframework.data.annotation.Id; import org.springframework.data.annotation.Version; import org.springframework.data.relational.core.mapping.Table; @Table("room_membership") public record RoomMembershipEntity(@Id UUID id,UUID roomId,UUID principalId,RoomRole role,Instant joinedAt,Instant leftAt,Instant updatedAt,@Version Long version) {}
