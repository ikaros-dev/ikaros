package run.ikaros.sharing; import java.time.Instant; import java.util.UUID; import org.springframework.data.annotation.Id; import org.springframework.data.annotation.Version; import org.springframework.data.relational.core.mapping.Table; @Table("room_message") public record RoomMessageEntity(@Id UUID id,UUID roomId,UUID authorId,String idempotencyKey,String body,Instant createdAt,@Version Long version) {}
