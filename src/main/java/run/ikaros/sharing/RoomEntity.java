package run.ikaros.sharing; import java.time.Instant; import java.util.UUID; import org.springframework.data.annotation.Id; import org.springframework.data.annotation.Version; import org.springframework.data.relational.core.mapping.Table; @Table("room") public record RoomEntity(@Id UUID id,UUID ownerId,String kind,String targetType,UUID targetId,String visibility,RoomStatus status,long stateVersion,long sequence,Instant expiresAt,Instant createdAt,Instant updatedAt,@Version Long version) {}
