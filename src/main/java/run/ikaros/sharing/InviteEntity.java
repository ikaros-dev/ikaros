package run.ikaros.sharing; import java.time.Instant; import java.util.UUID; import org.springframework.data.annotation.Id; import org.springframework.data.annotation.Version; import org.springframework.data.relational.core.mapping.Table; @Table("room_invite") public record InviteEntity(@Id UUID id,UUID roomId,UUID issuerId,UUID inviteeId,RoomRole role,String idempotencyKey,InviteStatus status,Instant expiresAt,Instant createdAt,Instant updatedAt,@Version Long version) {}
