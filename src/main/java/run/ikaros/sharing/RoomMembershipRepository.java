package run.ikaros.sharing; import java.util.UUID; import org.springframework.data.repository.reactive.ReactiveCrudRepository; import reactor.core.publisher.Flux; import reactor.core.publisher.Mono; public interface RoomMembershipRepository extends ReactiveCrudRepository<RoomMembershipEntity,UUID>{Flux<RoomMembershipEntity> findAllByRoomIdAndLeftAtIsNull(UUID roomId); Mono<RoomMembershipEntity> findByRoomIdAndPrincipalId(UUID roomId,UUID principalId);}
