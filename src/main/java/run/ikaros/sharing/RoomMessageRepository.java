package run.ikaros.sharing; import java.util.UUID; import org.springframework.data.repository.reactive.ReactiveCrudRepository; import reactor.core.publisher.Flux; import reactor.core.publisher.Mono; public interface RoomMessageRepository extends ReactiveCrudRepository<RoomMessageEntity,UUID>{Mono<RoomMessageEntity> findByRoomIdAndAuthorIdAndIdempotencyKey(UUID roomId,UUID authorId,String idempotencyKey);Flux<RoomMessageEntity> findAllByRoomIdOrderByCreatedAtAsc(UUID roomId);}
