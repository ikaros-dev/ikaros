package run.ikaros.notes; import java.time.Instant; import java.util.UUID; import org.springframework.data.annotation.Id; import org.springframework.data.annotation.Version; import org.springframework.data.relational.core.mapping.Table; @Table("private_note") public record PrivateNoteEntity(@Id UUID id,UUID vaultId,UUID notebookId,UUID ownerId,String encryptedTitle,String encryptedBody,String encryptedProperties,long revision,boolean tombstone,Instant createdAt,Instant updatedAt,@Version Long version) {}
