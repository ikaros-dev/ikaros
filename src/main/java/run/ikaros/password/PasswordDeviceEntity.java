package run.ikaros.password; import java.time.Instant; import java.util.UUID; import org.springframework.data.annotation.Id; import org.springframework.data.annotation.Version; import org.springframework.data.relational.core.mapping.Table; @Table("password_device") public record PasswordDeviceEntity(@Id UUID id,UUID ownerId,String name,String deviceType,DeviceTrustState trustState,Instant lastSyncAt,Instant lastUnlockAt,Instant createdAt,Instant updatedAt,@Version Long version) {}
