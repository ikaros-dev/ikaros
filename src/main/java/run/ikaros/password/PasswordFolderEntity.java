package run.ikaros.password; import java.time.Instant; import java.util.UUID; import org.springframework.data.annotation.Id; import org.springframework.data.annotation.Version; import org.springframework.data.relational.core.mapping.Table; @Table("password_folder") public record PasswordFolderEntity(@Id UUID id,UUID vaultId,UUID ownerId,UUID parentId,String encryptedName,Instant createdAt,@Version Long version) {}
