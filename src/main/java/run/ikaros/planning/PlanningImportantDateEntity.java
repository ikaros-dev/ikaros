package run.ikaros.planning; import java.time.Instant; import java.util.UUID; import org.springframework.data.annotation.Id; import org.springframework.data.annotation.Version; import org.springframework.data.relational.core.mapping.Column; import org.springframework.data.relational.core.mapping.Table;
@Table("planning_important_date") public record PlanningImportantDateEntity(@Id UUID id,@Column("owner_id") UUID ownerId,String title,String description,@Column("occurs_at") Instant occursAt,@Column("time_zone") String timeZone,String kind,PlanningImportantDateStatus status,@Column("created_at") Instant createdAt,@Column("updated_at") Instant updatedAt,@Version Long version) {}
