package run.ikaros.finance; import java.math.BigDecimal; import java.time.Instant; import java.util.UUID; import org.springframework.data.annotation.Id; import org.springframework.data.annotation.Version; import org.springframework.data.relational.core.mapping.Table; @Table("finance_account") public record AccountEntity(@Id UUID id,UUID ledgerId,UUID ownerId,String name,AccountType type,String currency,BigDecimal openingBalance,BigDecimal currentBalance,String institution,String maskedIdentifier,String credentialRef,boolean archived,Instant createdAt,Instant updatedAt,@Version Long version) {}
