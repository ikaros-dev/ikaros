package run.ikaros.finance; import java.math.BigDecimal; import java.time.Instant; import java.util.UUID; import org.springframework.data.annotation.Id; import org.springframework.data.annotation.Version; import org.springframework.data.relational.core.mapping.Table; @Table("finance_scheduled_transaction") public record ScheduledTransactionEntity(@Id UUID id,UUID ledgerId,UUID ownerId,String rule,Instant nextRun,TransactionType type,UUID accountId,UUID targetAccountId,BigDecimal amount,String currency,UUID categoryId,String payee,String note,boolean active,Instant createdAt,Instant updatedAt,@Version Long version) {}
