package run.ikaros.game; import java.util.UUID; import org.springframework.data.annotation.Id; import org.springframework.data.annotation.Version; import org.springframework.data.relational.core.mapping.Table; import org.springframework.data.relational.core.mapping.Column;
@Table("game_asset") public record GameAssetEntity(@Id UUID id,@Column("owner_id") UUID ownerId,@Column("game_id") UUID gameId,@Column("version_id") UUID versionId,@Column("attachment_id") UUID attachmentId,GameAssetCategory category,@Column("display_name") String displayName,@Column("checksum_algorithm") String checksumAlgorithm,@Column("checksum_value") String checksumValue,GameAssetAvailability availability,@Version Long entityVersion) {}
