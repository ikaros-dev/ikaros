spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/ikaros
    username: ikaros
    password: ikaros
  sql:
    init:
      mode: always
      platform: postgresql
