ikaros:
  plugin:
    runtime-mode: development
    fixed-plugin-path:
      - C:\\Develop\\ikaros-dev\\plugin-starter
    enabled-plugins:
      - plugin-starter

spring:
  thymeleaf:
    cache: false

logging:
  level:
    run.ikaros.server: DEBUG
springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
  show-actuator: true

management:
  endpoints:
    web:
      exposure:
        include: "*"
