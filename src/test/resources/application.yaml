ikaros:
  external-url: "http://${server.address:localhost}:${server.port}"

server:
  port: 51000
  compression:
    enabled: true
  error:
    whitelabel:
      enabled: false

spring:
  thymeleaf:
    cache: false
  sql:
    init:
      mode: always
      platform: h2
  codec:
    max-in-memory-size: 10MB
  r2dbc:
    url: r2dbc:h2:mem:///ikaros
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true


springdoc:
  api-docs:
    enabled: false

logging:
  level:
    run.ikaros.server: DEBUG
  file:
    name: ./build/logs/ikaros.log
  logback:
    rollingpolicy:
      max-file-size: 10MB
      total-size-cap: 1GB
      max-history: 2
