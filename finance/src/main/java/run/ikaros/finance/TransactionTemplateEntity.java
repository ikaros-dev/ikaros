package run.ikaros.finance; import java.math.BigDecimal; import java.time.Instant; import java.util.UUID; import org.springframework.data.annotation.Id; import org.springframework.data.annotation.Version; import org.springframework.data.relational.core.mapping.Table; @Table("finance_transaction_template") public record TransactionTemplateEntity(@Id UUID id,UUID ledgerId,UUID ownerId,String name,TransactionType type,UUID accountId,UUID targetAccountId,BigDecimal amount,String currency,UUID categoryId,String payee,String note,boolean archived,Instant createdAt,Instant updatedAt,@Version Long version) {}
