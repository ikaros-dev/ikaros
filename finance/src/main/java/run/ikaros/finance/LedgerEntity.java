package run.ikaros.finance; import java.time.Instant; import java.util.UUID; import org.springframework.data.annotation.Id; import org.springframework.data.annotation.Version; import org.springframework.data.relational.core.mapping.Table; @Table("finance_ledger") public record LedgerEntity(@Id UUID id,UUID ownerId,String name,String baseCurrency,boolean archived,Instant createdAt,Instant updatedAt,@Version Long version) {}
