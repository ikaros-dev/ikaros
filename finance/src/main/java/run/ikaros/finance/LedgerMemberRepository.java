package run.ikaros.finance; import java.util.UUID; import org.springframework.data.repository.reactive.ReactiveCrudRepository; import reactor.core.publisher.Flux; import reactor.core.publisher.Mono; public interface LedgerMemberRepository extends ReactiveCrudRepository<LedgerMemberEntity,UUID>{Flux<LedgerMemberEntity> findAllByLedgerIdOrderByCreatedAtAsc(UUID ledgerId);Mono<LedgerMemberEntity> findByLedgerIdAndPrincipalId(UUID ledgerId,UUID principalId);}
