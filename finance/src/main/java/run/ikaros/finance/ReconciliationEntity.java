package run.ikaros.finance; import java.math.BigDecimal; import java.time.Instant; import java.time.LocalDate; import java.util.UUID; import org.springframework.data.annotation.Id; import org.springframework.data.annotation.Version; import org.springframework.data.relational.core.mapping.Table; @Table("finance_reconciliation") public record ReconciliationEntity(@Id UUID id,UUID ledgerId,UUID accountId,UUID ownerId,LocalDate statementDate,BigDecimal statementBalance,BigDecimal calculatedBalance,BigDecimal difference,ReconciliationStatus status,String note,Instant createdAt,Instant updatedAt,@Version Long version) {}
