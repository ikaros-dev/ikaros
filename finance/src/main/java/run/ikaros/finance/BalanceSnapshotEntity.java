package run.ikaros.finance; import java.math.BigDecimal; import java.time.Instant; import java.time.LocalDate; import java.util.UUID; import org.springframework.data.annotation.Id; import org.springframework.data.annotation.Version; import org.springframework.data.relational.core.mapping.Table; @Table("finance_balance_snapshot") public record BalanceSnapshotEntity(@Id UUID id,UUID ledgerId,UUID accountId,UUID ownerId,LocalDate snapshotDate,BigDecimal balance,String currency,Instant createdAt,@Version Long version) {}
