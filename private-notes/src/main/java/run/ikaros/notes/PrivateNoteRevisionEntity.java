package run.ikaros.notes; import java.time.Instant; import java.util.UUID; import org.springframework.data.annotation.Id; import org.springframework.data.relational.core.mapping.Table; @Table("private_note_revision") public record PrivateNoteRevisionEntity(@Id UUID id,UUID noteId,UUID vaultId,UUID ownerId,long revision,String encryptedTitle,String encryptedBody,String encryptedProperties,Instant createdAt) {}
