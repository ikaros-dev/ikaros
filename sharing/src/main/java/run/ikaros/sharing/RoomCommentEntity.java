package run.ikaros.sharing; import java.time.Instant; import java.util.UUID; import org.springframework.data.annotation.Id; import org.springframework.data.annotation.Version; import org.springframework.data.relational.core.mapping.Table; @Table("room_comment") public record RoomCommentEntity(@Id UUID id,UUID roomId,UUID authorId,String idempotencyKey,String body,boolean deleted,Instant createdAt,Instant updatedAt,@Version Long version) {}
